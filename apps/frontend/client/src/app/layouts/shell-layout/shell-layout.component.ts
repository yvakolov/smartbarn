import { ChangeDetectionStrategy, Component, OnDestroy, inject, signal } from '@angular/core';
import { Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';
import { TranslocoPipe } from '@jsverse/transloco';
import { lucideArrowDownUp,lucideChevronRight,lucideHouse,lucideLibrary,lucidePanelLeft,lucidePlus,lucideSettings,lucideX } from '@smartbarn/icons';
import { IconComponent, provideIcons } from '@smartbarn/ui-kit';
import { BUILDING_STRUCTURE_CHANGED_EVENT, loadBuildingStructure, rebuildStoreyElevations, saveBuildingStructure, storeyFloorDatumElevationMm, storeyTopElevationMm, type BuildingStructureModel } from '../../features/building-structure/building-structure';
import { FLOOR_FIELD_CHANGED_EVENT } from '../../features/floor-field/floor-field.store';
import { MATERIAL_CATALOG_CHANGED_EVENT, MATERIAL_GROUPS, loadMaterialCatalog, type MaterialGroup, type MaterialRecord } from '../../features/materials/material-catalog';
type Workspace='house'|'materials'|'exchange'|'settings';
@Component({selector:'sb-shell-layout',standalone:true,imports:[RouterOutlet,RouterLink,RouterLinkActive,TranslocoPipe,IconComponent],providers:[...provideIcons(lucidePanelLeft,lucideHouse,lucideLibrary,lucideArrowDownUp,lucideSettings,lucideChevronRight,lucidePlus,lucideX)],template:`
<div class="h-dvh overflow-hidden bg-[var(--sb-bg)] text-[var(--sb-text)]"><header class="flex h-14 items-center border-b border-[var(--sb-border)] bg-[var(--sb-surface)] px-3"><button class="mr-2 flex items-center justify-center rounded px-2 py-1 text-xl hover:bg-[var(--sb-gray-3)]" (click)="toggleSidebar()" [title]="'shell.navigation'|transloco"><sb-icon name="panelLeft" /></button><strong class="text-sm">{{'app.name'|transloco}}</strong></header><div class="grid h-[calc(100dvh-56px)] transition-[grid-template-columns] duration-150" [style.grid-template-columns]="sidebarOpen()?'52px 260px minmax(0,1fr)':'52px 0 minmax(0,1fr)'"><aside class="relative z-20 border-r border-[var(--sb-border)] bg-[var(--sb-surface)]"><nav class="flex h-full w-[52px] flex-col items-center gap-1 py-2">@for(space of spaces;track space.id){<button class="group relative flex h-11 w-11 items-center justify-center rounded-lg text-xl" [class.mt-auto]="space.id==='settings'" [class.bg-[var(--sb-accent-soft)]]="activeSpace()===space.id" (click)="selectSpace(space.id)" [attr.aria-label]="space.labelKey|transloco"><sb-icon [name]="space.icon" /></button>}</nav></aside><aside class="overflow-hidden border-r border-[var(--sb-border)] bg-[var(--sb-surface)]"><nav class="flex h-full w-[260px] flex-col gap-1 overflow-auto p-3 text-sm"><div class="mb-2 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[var(--sb-text-muted)]">{{spaceTitleKey()|transloco}}</div>@switch(activeSpace()){@case('house'){
<a routerLink="/app/profile" routerLinkActive="bg-[var(--sb-accent-soft)] text-[var(--sb-text)]" class="block rounded px-3 py-2 font-medium text-[var(--sb-text-muted)]">Профиль</a>
<a routerLink="/app/foundation" routerLinkActive="bg-[var(--sb-accent-soft)] text-[var(--sb-text)]" class="block rounded px-3 py-2 text-[var(--sb-text-muted)]">{{'structure.foundation'|transloco}}</a><details class="group/tree rounded" open><summary class="cursor-pointer list-none rounded px-3 py-2 text-[var(--sb-text-muted)] hover:bg-[var(--sb-gray-3)]"><sb-icon name="chevronRight" class="mr-1 transition-transform group-open/tree:rotate-90" />{{'structure.storeys'|transloco}}</summary><div class="ml-5 border-l border-[var(--sb-border)] pl-2"><div class="mb-1 flex items-center gap-1 px-1"><a routerLink="/app/storeys" routerLinkActive="bg-[var(--sb-accent-soft)] text-[var(--sb-text)]" class="min-w-0 flex-1 rounded px-2 py-1.5 text-xs text-[var(--sb-text-muted)]">{{'structure.manageStoreys'|transloco}}</a><button type="button" class="flex h-7 w-7 shrink-0 items-center justify-center rounded text-[var(--sb-text-muted)] hover:bg-[var(--sb-gray-3)]" title="Добавить этаж" aria-label="Добавить этаж" (click)="addStorey()"><sb-icon name="plus" /></button></div>@for(storey of buildingStructure().storeys;track storey.id;let i=$index){<details class="group/storey rounded" open><summary class="flex cursor-pointer list-none items-center rounded px-3 py-1.5 text-[var(--sb-text-muted)] hover:bg-[var(--sb-gray-3)]"><sb-icon name="chevronRight" class="mr-1 transition-transform group-open/storey:rotate-90" /><span class="min-w-0 flex-1">{{'structure.storey'|transloco}} {{i+1}}</span><span class="mr-1 text-[10px] opacity-70">{{formatElevation(storeyDatum(storey.id))}} / {{formatElevation(storeyTop(storey.id))}} м</span><button type="button" class="flex h-6 w-6 shrink-0 items-center justify-center rounded hover:bg-[var(--sb-gray-3)] disabled:cursor-not-allowed disabled:opacity-25" title="Удалить этаж" aria-label="Удалить этаж" [disabled]="buildingStructure().storeys.length===1" (click)="removeStorey(storey.id,$event)"><sb-icon name="x" /></button></summary><div class="ml-5 border-l border-[var(--sb-border)] pl-2"><a routerLink="/app/floor-field" [queryParams]="{storey:storey.id}" routerLinkActive="bg-[var(--sb-accent-soft)] text-[var(--sb-text)]" class="block rounded px-3 py-1.5 text-xs text-[var(--sb-text-muted)]">{{'shell.floorField'|transloco}}</a><a routerLink="/app/walls" [queryParams]="{storey:storey.id}" routerLinkActive="bg-[var(--sb-accent-soft)] text-[var(--sb-text)]" class="block rounded px-3 py-1.5 text-xs text-[var(--sb-text-muted)]">{{'shell.walls'|transloco}}</a></div></details>}</div></details><div class="rounded px-3 py-2 text-[var(--sb-text-muted)] opacity-70">{{'shell.roof'|transloco}} <span class="float-right text-xs">{{'shell.soon'|transloco}}</span></div>}@case('materials'){@for(group of materialGroups;track group.id){<details class="group/tree rounded" open><summary class="cursor-pointer list-none rounded px-3 py-2 text-[var(--sb-text-muted)]"><sb-icon name="chevronRight" />{{('materials.groups.'+group.id)|transloco}}</summary><div class="ml-5 border-l border-[var(--sb-border)] pl-2">@for(material of materialsForGroup(group.id);track material.id){<a routerLink="/app/materials" [queryParams]="{group:group.id,material:material.id}" class="block truncate rounded px-3 py-1.5 text-xs text-[var(--sb-text-muted)]">{{material.name}}</a>}</div></details>}}@case('exchange'){<a routerLink="/app/exchange/import" class="rounded px-3 py-2 text-[var(--sb-text-muted)]">{{'shell.import'|transloco}}</a><a routerLink="/app/exchange/export" class="rounded px-3 py-2 text-[var(--sb-text-muted)]">{{'shell.export'|transloco}}</a>}@case('settings'){<a routerLink="/app/settings" class="rounded px-3 py-2 text-[var(--sb-text-muted)]">{{'shell.general'|transloco}}</a>}}</nav></aside><main class="min-h-0 min-w-0 overflow-hidden"><router-outlet/></main></div></div>`,changeDetection:ChangeDetectionStrategy.OnPush})
export class ShellLayoutComponent implements OnDestroy{private readonly router=inject(Router);readonly sidebarOpen=signal(true);readonly activeSpace=signal<Workspace>(this.spaceFromUrl(this.router.url));readonly touchLabelKey=signal('');readonly materialGroups=MATERIAL_GROUPS;readonly materials=signal<MaterialRecord[]>(loadMaterialCatalog());readonly buildingStructure=signal<BuildingStructureModel>(loadBuildingStructure());private touchTimer?:ReturnType<typeof setTimeout>;private readonly refreshMaterials=()=>this.materials.set(loadMaterialCatalog());private readonly refreshBuildingStructure=()=>this.buildingStructure.set(loadBuildingStructure());readonly spaces:ReadonlyArray<{id:Workspace;labelKey:string;icon:string}>=[{id:'house',labelKey:'shell.house',icon:'house'},{id:'materials',labelKey:'shell.materials',icon:'library'},{id:'exchange',labelKey:'shell.exchange',icon:'arrowDownUp'},{id:'settings',labelKey:'shell.settings',icon:'settings'}];constructor(){if(typeof window!=='undefined'){window.addEventListener(MATERIAL_CATALOG_CHANGED_EVENT,this.refreshMaterials);window.addEventListener(BUILDING_STRUCTURE_CHANGED_EVENT,this.refreshBuildingStructure);window.addEventListener(FLOOR_FIELD_CHANGED_EVENT,this.refreshBuildingStructure);}}ngOnDestroy(){if(typeof window!=='undefined'){window.removeEventListener(MATERIAL_CATALOG_CHANGED_EVENT,this.refreshMaterials);window.removeEventListener(BUILDING_STRUCTURE_CHANGED_EVENT,this.refreshBuildingStructure);window.removeEventListener(FLOOR_FIELD_CHANGED_EVENT,this.refreshBuildingStructure);}if(this.touchTimer)clearTimeout(this.touchTimer);}toggleSidebar(){this.sidebarOpen.update(v=>!v);}selectSpace(s:Workspace){this.activeSpace.set(s);if(s==='materials')this.refreshMaterials();if(s==='house')this.refreshBuildingStructure();void this.router.navigateByUrl(this.defaultUrl(s));}spaceTitleKey(){return this.labelKeyForSpace(this.activeSpace());}materialsForGroup(group:MaterialGroup){return this.materials().filter(material=>material.group===group);}storeyDatum(id:string){const storey=this.buildingStructure().storeys.find(item=>item.id===id);return storey?storeyFloorDatumElevationMm(storey):0;}storeyTop(id:string){const storey=this.buildingStructure().storeys.find(item=>item.id===id);return storey?storeyTopElevationMm(storey):0;}formatElevation(mm:number){const value=mm/1000;if(Math.abs(value)<.0005)return'0.000';return`${value>0?'+':''}${Number(value.toFixed(3))}`;}addStorey(){const current=loadBuildingStructure();const used=new Set(current.storeys.map(s=>s.id));let number=current.storeys.length+1;while(used.has(`storey-${number}`))number++;const previous=current.storeys.at(-1);const clearanceMm=previous?.clearanceMm??2800;const storeys=[...current.storeys,{id:`storey-${number}`,name:`Этаж ${number}`,baseElevationMm:0,clearanceMm,heightMm:clearanceMm+224}];saveBuildingStructure(rebuildStoreyElevations({...current,storeys}));this.refreshBuildingStructure();}removeStorey(id:string,event:Event){event.preventDefault();event.stopPropagation();const current=loadBuildingStructure();if(current.storeys.length<=1)return;const storeys=current.storeys.filter(s=>s.id!==id);saveBuildingStructure(rebuildStoreyElevations({...current,storeys}));this.refreshBuildingStructure();}private defaultUrl(s:Workspace){return s==='house'?'/app/profile':s==='materials'?'/app/materials':s==='exchange'?'/app/exchange/import':'/app/settings';}private labelKeyForSpace(s:Workspace){return this.spaces.find(x=>x.id===s)?.labelKey??'';}private spaceFromUrl(url:string):Workspace{return url.includes('/materials')?'materials':url.includes('/exchange')||url.includes('/import-export')?'exchange':url.includes('/settings')?'settings':'house';}}
